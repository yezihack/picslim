# TODO

## BUG

位置：输入与输出
现状：输入目录 / 文件，重新选择时，下面”已选择 9 张图片，预计节省 2.3 MB（约 -45%）“值还是上一次的结果。
预期：输入目录 / 文件，重新选择时，下面”已选择 9 张图片，预计节省 2.3 MB（约 -45%）“值重新计算。
解决否：解决

位置：输入与输出
现状：输入目录 / 文件，选择一个目录时，输出目录：显示”输出目录（默认自动创建，可手动修改）“
预期：输出目录自动显示输入目录，命名规则：支持输出目录配置（自定义或自动创建默认输入的同目录，如输入目录：d:/image01，输出目录：d:/image01-compressor 若遇到目录存在则后加个4位日期，分秒，如0123，即：d:/image01-compressor-0123）
解决否：解决

位置：输入与输出
现状：选择输入时，下面”已选择 3769 张图片“没有这个目录图片的总大小。
期望：选择输入后，显示总图片大小。
解决否：解决

位置：输入与输出，任务进度
现状：点击”开始压缩”无反应。
期望：开始压缩则开始工作。读取输入目录下的图片，压缩后输出到输出目录，右边显示任务进度。任务进度参考图片。
解决否：解决

位置：压缩前后预览对比（展示图片）
现状：
期望：能预览压缩前后的图片对比。打开输出的目录。导出CSV报告。
解决否：OK


位置：右上角设置
现状：点击设置没反应。
期望：点击设置，可以设置并发数量，其它信息自行决定。
解决：

位置：LOGO位置
现状：无LOGo显示
期望：创建一个有代表的LOGO，放在右上角。并在桌面安装也使用这个图标。状态栏上也显示图标。
解决：